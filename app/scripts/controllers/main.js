'use strict';

/**
    fizzBuzz returns given argument
    unless multiple of 3 (then returns 'fizz')
           multiple of 5 (then returns 'buzz')
           multiple of both 3 & 5 (then returns 'fizzbuzz')
 */
angular.module('fizzbuzzApp')
    .controller('MainCtrl', function ($scope) {

        $scope.fizzBuzz = function(fizzVariable) {

        };

    });
